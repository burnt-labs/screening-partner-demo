{
  "name": "screening-partner-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Demo app for the Screening by Burnt Partner API (Model A/B) — provision a unit + screening, start a no-login applicant screening, and poll the application-group status/decision.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
